spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  sql:
    init:
      mode: never
app:
  jwt:
    secret: test-secret-key-for-integration-tests-which-is-long-enough
    exp-minutes: 60